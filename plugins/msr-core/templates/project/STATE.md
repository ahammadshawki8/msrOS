# Project State

<!-- msr:digest:start -->
**Project:** <name> | **Archetype:** <archetype> | **Updated:** <YYYY-MM-DD>
**Now:** <what is in flight right now, one line>
**Next:** <the single next concrete task, one line>
**Blocked:** none
<!-- msr:digest:end -->

<!--
  Everything above the end marker is injected into EVERY future session by the
  SessionStart hook. Keep it to those four lines.

  Everything below loads only when /msr-session-start or /msr-handoff reads this
  file. Put detail here.
-->

## Decisions

Choices that would otherwise get re-litigated. Record the reason, not just the choice —
the code already shows what you picked, never what you rejected.

- [D1] <decision> — <why, including what was rejected>

## Open threads

Discovered and deliberately deferred. Delete entries when they are resolved.

- [T1] <thread>

## Recent changes

Keep the last ten. Delete older entries; `git log` is the permanent record.

- [C1] <YYYY-MM-DD> — <what changed and its effect>
